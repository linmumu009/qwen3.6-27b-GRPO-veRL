# 验收状态

更新时间：2026-09-21。五类训练均在5号机完成真实1步运行，退出码0、检查点结构通过；SFT的HF导出通过。CPT成品模型待用户提供，异机迁移尚未验证。

|项目|状态|
|---|---|
|SFT|真实1步训练成功，32权重分片；训练loss 1.5363、非零梯度，验证loss 0.8809|
|trajory-SFT|真实1步训练成功，32权重分片；训练loss 0.00516、非零梯度，验证loss 0.000164|
|CPT|真实1步训练成功，32权重分片；训练loss 2.7324、非零梯度，验证loss 2.6585；原文监督预检通过|
|GRPO|真实1步流程成功，32权重分片，退出码0；样例奖励均为1、梯度范数为0，仅证明流程走通|
|agentic-GRPO|真实1步流程成功，32权重分片，退出码0；16个独立实例各完成1次真实计算器调用，全部成功；奖励均为1、梯度范数为0|
|原始模型|实体文件与逐文件SHA256校验已完成|
|CPT模型|等待用户提供|
|共享源码与补丁|四份源码归档完成|
|离线镜像|已导出16GB基础镜像，镜像ID和归档摘要已核验|
|HF导出|SFT检查点导出成功：1199个张量、15个分片；无缺失、多余或形状不符的权重。尚未验证导出模型推理|
|异机复验|未执行|

2026-09-21按用户要求清理了本次验收的检查点、HF测试导出、原始日志、缓存及临时文件。下列结果及verification.json保留的是清理前的验证汇总，所列运行目录已删除；清理记录见cleanup_20260921.json。所有样例仅证明工程流程，不代表业务训练收益。

## 已确认的运行环境问题

- 独立容器需保留CANN的Python路径、提供足够Ray CPU配额，并将临时目录放到数据盘。
- 完整RL进程的锁页主机内存分配报207001。交付版改用普通主机内存和同步权重拷贝；独立ACL拷贝往返检查通过。vLLM输入缓冲及休眠备份采用相同兼容路径。
- 历史vLLM地址适配使用物理设备编号，与本包TP4副本的局部编号不一致，导致权重同步互等。已增加仅适用于当前TP副本布局的修复和地址对应回归检查。
- CP2位置索引改用普通内存与同步传输，两个分区的索引取值及梯度单独检查通过；完整GRPO随后成功。
- 汇总保留失败或中止运行的真实状态，不算作训练通过。无设备容器中的HF导出失败状态也保留；标准入口中的后续导出已通过。原始运行文件已按要求清理。

两类GRPO样例较简单，同题各候选答案均正确，组内优势为0。本次不证明模型获得能力提升；业务训练需用能产生有效奖励差异的数据并另行评测。

## 历史成功运行索引（产物已删除）

以下是清理前的历史运行标识，不是当前可访问路径。若需要新的检查点或导出模型，请重新运行对应训练入口。

|类型|成功运行目录|
|---|---|
|SFT|outputs/SFT/20260921-105153-3174517|
|trajory-SFT|outputs/trajory-SFT/20260921-110011-3293249|
|CPT|outputs/CPT/20260921-110711-3398996|
|GRPO|outputs/GRPO/20260921-124216-4167216|
|agentic-GRPO|outputs/agentic-GRPO/20260921-125837-165151|
|SFT HF导出|outputs/SFT/20260921-120116-3847655/hf_export|

本地7项契约检查通过；5号机数据/配置预检、同步ACL往返拷贝、CP2位置索引及梯度检查通过。完整原始模型和离线镜像已核验摘要，第三方源码及交付补丁具有版本与摘要清单。未做吞吐优化、业务效果评测或异机完整训练验收。
