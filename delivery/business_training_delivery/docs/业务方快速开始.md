# 业务方快速开始

请按[客户训练操作手册](业务方运行手册.md)完成三步：

1. 将新数据放入对应 `datasets/训练类型/` 下的 `train.jsonl` 和 `val.jsonl`。
2. 修改对应 `Train/训练类型/config.yaml` 中的训练轮次、步数限制及超时。
3. 执行对应 `bash Train/训练类型/run.sh`。

具体目录、参数值和五类命令都在操作手册中。服务器部署、镜像和模型导出见[运维说明](运维部署与验收说明.md)。
