# CPT 两阶段实验：冻结协议与执行账本

日期：2026-09-10。用户已授权按关联任务末尾的两阶段计划实施。当前8遍训练完成、逐遍评测进行中；第二阶段已准备关联语料，尚未训练，不能宣称实验完成或收益成立。

## 最新状态（2026-09-10，首次自动接续）

- 232步、8个完整检查点独立核验通过；每遍336,702输入token、总计2,693,616，与预算完全一致。逐遍平均训练loss为2.0453、1.9677、1.9066、1.8743、1.8312、1.7906、1.7680、1.7510；这是书籍训练损失，不是知识问答收益。
- v2接续成功通过检查点门禁，但基线评测在生成前因`/vllm`源码根目录未加入Python路径失败；无任何回答、不更改评测协议。独立`code_v3`补齐推理库路径并保留Ascend路径与旧日志，PID 1615050；`launch_v3.sh --after-training --log-suffix=-v3`已恢复评测。
- 第0遍完整5016请求完成、零截断，多数票SC=189/226、Logistika=1180/1446；正在导出第1遍。后续检查`status.safe.json`和`*-v3.log`，不要重跑训练或重复基线。
- 第二阶段候选位于`stage2_preparation/prepared_v2`：重建原语料逐条正文完全一致；将4685完整段落组织为254个同章单元，再重组116条记录，全部段落覆盖一次，无删改正文。输入338,433、有效监督338,317，较原臂增加0.5143%，通过≤1%总预算门槛；最长4094含EOS。**实际每步token分布尚未匹配，必须进一步核算，不能把总量接近写成每步相同。**
- 词面关联初版发现月份/图表词干扰，保留v1记录；v2排除月份词参与相似度，并在弱关联时优先保持同章自然上下文，80/116组具有跨章cosine≥0.10。
- 固定分层12组内容抽检：组75/97/108/50/7/52/56/73为相关概念或条件的互补语境；组2/42只有较宽泛的同主题联系；组111/60以原教材近重复表述为主。没有新增事实或答案，但不能把80组都称为已验证的跨章推理监督，抽检也不代表全量语义认证。未以基准题选择或调整分块。
- CPU实测现有TP4/CP2打包接口：两条跨章样本的段内位置连续、章节标记不新建序列、两CP分片合并后每个真实token恰好出现一次、样本间边界保持。服务器版本只支持zigzag，首次审计因误用本地新版`cp_layout`参数失败，独立`audit_code_v2`已按实装接口通过。此项是打包契约审核，**不冒充NPU注意力内核数值验证**；完整注意力核验仍待补齐。
- [安全证据](cpt_two_experiments_preparation_20260910.safe.json)含训练和制数计数、哈希与CPU打包结果。所有正文保留远端私有目录；制数/审核无API调用。关联语料尚不允许训练：需补每步预算、实际注意力核验及第二阶段存储和两臂启动合同。

## 第一阶段：原分块曝光曲线

- 起点：原始 Step120 的模型态，fresh Adam；不使用旧CPT、SFT或题面污染候选。
- 语料：旧116条教材块原样复用；Parquet SHA256 `975dc8719da2ff6ba0f2eb5db4faa03cb72f2f1ee36c14ed5e35460d4ca4c1c2`。每遍336,702输入token、336,586有效监督token，batch=4、每遍29步。
- 预算：固定8遍、232步、累计2,693,616输入token；不根据中途得分修改学习率或延长。
- 学习率：峰值5e-7、最低1e-7，232步余弦衰减，2步预热。显式Adam beta=(0.9,0.999)，对齐旧运行实际生效值（旧generic betas不是有效覆盖）。权重衰减0、clip=1；TP4/PP2/CP2，16张NPU。视觉及MTP冻结。
- 每29步保留model、optimizer（含scheduler）、extra（RNG）和dataloader状态，共8个检查点；不清理历史检查点。启动前检查专用盘空闲至少3.65TB。保存文件完整性门禁不等于恢复等价性实测，第一检查点后仍须检查实际状态并补恢复核验。
- 为保持优化器在单进程组中连续运行，先连续训练8遍，再依次评测第0至8遍；不采用每遍重启优化器的方式。完整保留每个曲线点。
- 每点两套完整基准：SC-bench知识226题、LogistikaBench1446题；原提示、关闭thinking、temperature=0、seed=1024、max_tokens=96、max_model_len=8192、TP8、并发32。每点3轮，共45,144请求；截断/无效计错。保存输入token哈希并断言实际输出关联的prompt_token_ids相同。
- 逐题严格多数票，分别报告两套分数、相对Step120的改对/改错；基准作为模型选择集，不声称独立测试。任何曲线峰值只限本训练配置、已测范围。

## 第二阶段：原分块 vs 跨章节关联分块

第一阶段结束后接续，不因曲线不涨取消。本轮固定选择原始Step120作为两臂共同起点，避免按验证峰值更换起点；两臂fresh Adam，使用相同种子、学习率计划、每步预算和训练总量。计划各4遍、116步，峰值5e-7至1e-7、2步预热。原分块臂必须新跑，不把旧4遍曲线或只追加关联臂当对照。

关联分块仅使用原授权教材和来源信息，不读取评测问题/答案来决定关联。先恢复清洗后的完整段落，以教材术语相似度寻找跨章候选，保留来源边界，禁止凭相似度添加事实、推论或模型生成说明。数据构造和审核不调用外部API。语义关联质量须抽检，若不能支持关联机制则修复组织程序，不能以随机拼接冒充关联。

启动门禁：原文覆盖和重复计数完整；每条≤4096含EOS；116条/batch4/29步每遍，正文和实际监督token相对原臂差≤1%，同时报告标题/分隔符/EOS和长度分布。不能用截掉正文或复制样本凑平预算。预算不能匹配时先修复制数，不悄悄放宽门槛。验证同一条样本中后段可关注前段，跨样本仍隔离。数据和规则在两臂训练前同时冻结；存储预算重新检查。

两臂按第一阶段同一完整评测协议比较，分别报告两套逐题得失和配对不确定性。候选、原Step120做既有314题通用回归和隔离Agent回归；未完成或回归不通过不交付。单种子结果按探索实验解释。若无满足双基准不退步的候选，仍完成机制对照并报告无收益，不能只选合计峰值宣称成功。新的最终候选和邻近轮次须额外重复复核，不能把峰值选择用的三轮重复当独立确认。

## 运行位置及继续入口

- SSH：`ssh -F "C:/Users/Liu Lin/.ssh/config" huawei-05`；容器`llin-verl-trainer-m05-20260730`。
- 容器：`/workspace/llin-verl-grpo/runs/cpt-controlled-storage-20260910/llin/cpt-controlled-20260910`。
- 对应宿主机持久化路径：`/data4/llin/cpt-controlled-20260910`；数据盘在现有容器内额外挂载，容器重启后要先核实挂载。不得把未挂载目录误当空的新运行。
- `status.safe.json`为原子状态；`training_8x.log`及`curve8x/torchrun_logs`为训练证据；`curve8x/curve.safe.json`完成后是全部曲线汇总。
- 固定训练代码副本：`code/scripts`及`code_manifest.safe.json`。不要覆盖运行中的副本。先检查PID及状态再启动，禁止重复运行。
- 当前启动PID（容器）：1585763；语料tokenization gate已通过，实测完成第一遍29/232步、336,702输入token；首个检查点383,215,466,147 bytes，修正后的完整状态门禁实际通过。完整恢复等价性仍待实测。
- 首个检查点真实清单使用`rng_state`、`lr_scheduler`，原衔接门禁误取`extra`路径。独立`code_v2/scripts`副本已修正并通过实物核验；等待接续进程PID 1607285，`launch_v2.sh --after-training`以阻塞资源锁等待原流水线自然退出。**不终止训练、不覆盖原代码、不重启优化器**。原流水线训练后预计在旧门禁处抛出KeyError，v2随后复核8个完整检查点并自动导出/评测。应检查v2状态而非把这个已识别的旧门禁异常误判为训练失败；若缺最终232步检查点，v2仍会报错停下。
- 用户已明确授权每30分钟自动检查并接续第二阶段训练、验证及仓库提交推送。自动跟进ID为`cpt`，仅实质失败、需介入或全部完成时通知。
- 第一阶段代码：[训练入口](../scripts/run_logistics_cpt_curve_8x.sh)、[训练/导出/评测流水线](../scripts/run_logistics_cpt_curve_8x.py)。3项定向测试覆盖缺失恢复状态、无效多数票、重复请求和配对身份不匹配；远端shell语法检查通过。

## 下一次执行

1. 检查v3评测进程、导出/评测日志和磁盘增长，不重新启动已完成的8遍训练；8个完整检查点已经核验通过。
2. 保存8遍曲线完整结果，验证输出与题目契约及全部45,144请求；复核候选和相邻轮次。
3. 关联制数v2及CPU打包审核已完成；补每步token预算、NPU注意力/恢复核验，核算第二阶段存储（第一阶段8份HF导出后原专用盘余量有限，可用5号机/data1专用目录），冻结两臂启动合同后训练和评测；按情况完成通用/Agent回归及最终报告。
4. 本文件持续更新实际进展；每个已验证仓库更新同时更新README、提交并推送origin/main，只提交本任务文件。
