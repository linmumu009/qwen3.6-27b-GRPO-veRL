# 供应链问答整体重构：执行账本

日期：2026-09-09。**最新用户方向：重新整体考虑，暂停追加训练和QA扩量，先完成证据复审与最小诊断。** 新SFT73步及领域/通用验证已完成，Agent仍未跑，不晋级新模型。当前入口为[整体复审](supply_chain_rethink_20260909.md)；下文保留历史执行记录，不按旧待办自动恢复训练。无训练四条件诊断运行目录`runs/supply-chain-prompt-factorial-20260909-02`，先前01因启动脚本覆盖Ascend的PYTHONPATH导致缺acl、零回答失败，保留原日志；02只恢复环境路径继承，其余协议不变。下一次自动跟进检查02结果并做因素配对分析，不重复制数或训练。

## 目标与边界

提升供应链回答问题的实际能力，以固定SC-bench知识226题和LogistikaBench1446题观察收益，保留Step120原Agent/通用能力。评测集已反复观察，不称未触碰测试。不得用题干、答案、派生改写或定向证据制数；不得把公开可读资料直接视为可商业训练授权。当前可训练资料是用户授权的第8版物流教材，其他资料只读调查。

本轮不是对355道短答做更多轮次，也不是再修6道诊断题。顺序为：全量任务/错误分布和资料覆盖 → 找可教且重要的能力缺口 → 组织新任务形式与独立开发检查 → 冻结有边界的训练方案 → 训练 → 领域与原能力回归。不得在官方分数上选checkpoint或反复搜索提示。不能保证收益，训练和验证完成不等于目标达成。

## 已完成工作

1. 读取并继承9月8日[全局机制诊断](logistics_strategy_diagnostic_20260908.md)，不重跑：96题×两模型×三条件×三重复，另有3题证据对照。诊断支持调查候选匹配、概念辨析、条件核查，但不证明单一知识/推理缺陷。旧正文复核权限不无限沿用。
2. 核实9月9日同配置补测已实际完成。CPT1368、SFT1369；6改对5改错，稳定子集1改对3改错。六轮每轮1672题身份一致；输出上限均96。CPT三次1366/1367/1372，SFT1368/1369/1370；SFT第三轮1次解析失败保留计错。两套分别为Logistika1179→1179，SC189→190。旧多数票request默认128是元数据缺陷，逐轮96才是实际参数。
3. 收尾shell因上次在运行中更新而出现`output: command not found`，旧status保留failed作为事故记录；推理及多数票文件完整，不需重跑。以后将脚本复制到不可变运行路径，开跑后不覆盖它。
4. 完成**全部115条有章节教材块、全部436条候选、实际355条SFT QA**的分类盘点。百炼qwen3.8-max，并发64，137次调用、0重试、0结构失败，457025输入+39137输出=496162计量tokens；不是现金账单。没有生成新训练数据。
5. 将标签和完整1672题匿名结果按领域×题型×候选池规模汇总。Step120历史1369、同轮CPT1368、SFT1369，不把历史Step120当同期随机对照。所有题库SHA一致。安全结果：[覆盖与错误分层](supply_chain_global_coverage_20260909.safe.json)。

## 当前全量证据及正确解释

实际355训练题AI主领域分类：物料搬运19、仓储45、运输101、供应链协同58、库存计划22、采购16、财务绩效40、信息系统19、逆向/可持续16、其他19。物料搬运只占5.35%，但同轮CPT该官方类别125错，占304错的41.12%。其125错中93来自305道长候选表题，普通391题仅32错。

这不是建议按41%机械配比物料搬运：官方类别与教材分类不是经过专家校准的同义体系；长候选表还混合术语覆盖、匹配行为、重复选项和题库质量。最直接的工程结论是**不能继续把“增加一般短答题”等同于补足主要目标能力**。下一批必须明确教授什么，且要有独立测试能检验它。

355题的能力标签（允许重叠）：回忆99、辨析81、条件应用119、计算51、多步决策15、其他4。原始生成kind与“实际需要什么能力”并非同一口径；不能直接相减作错误率。当前教材QA是自由短答，缺乏与长候选匹配/多选逐项核查对应的明确监督任务。

115教材块主领域：物料搬运8、仓储14、运输29、供应链协同28、库存8、采购6、财务8、信息6、逆向4、其他4。自动分类把92块标成partial，22 explicit、1 none；这可能反映截断、图表和引用，不是证明全书92块无价值。**需按完整章节/跨块上下文恢复可验证知识单元，不能拿关键词命中率当知识覆盖率。** QA分类输入没有完整来源，所谓explicit标签只表示QA表达自足，绝不冒充来源真实性认证。

## 接下来必须依次实施

### 2026-09-09 自动跟进：整章知识单元提取预算

覆盖任务已结束，无同名进程。115章内块按文件顺序无损拼接为44章，最大章节74458字符；保留全部文字而不是再用单块命中代替来源充分性。

下一批已登记：`extract_book_learning_units.py`，44章每章最多12个不同知识单元，最多528单元；每章生成一次、独立来源审核一次，**最多88次百炼调用**，最大并发44，每次输出上限8000 tokens，0重试。先跑1–4章，至少8个通过单元才继续其余40章。输入仅整章授权教材，不含评测题/答案/模型错题。费用按返回usage另记，未设置或声称现金金额。

每个单元要有概念、定义、区分特征/条件、可选对比和公式、有效来源ID及逐字引用；引用做机械子串校验，所有语义声明再由独立调用审核。图表缺失/不支持断言拒绝。开发章节先冻结为4/15/19/21/26/37，后续跨章同概念与变体去重必须开发优先隔离。它们是SFT留出，不是CPT未见知识。

该步骤产出是可教知识单元而非直接训练数据。完成后须检查跨章重复、引用与条件充分性，再生成匹配/条件判别/多选任务及必要短答；不得跳过任务审核就训练。运行路径计划为`runs/supply-chain-learning-units-20260909-01`，不可变脚本放在`/opt/llin-learning-unit-code-20260909-01/`，进度以summary.safe.json为准。

#### 本次执行结果与下一恢复动作

2026-09-09用户催促后即时执行恢复批次：`recover_book_learning_units.py`，最多44调用，44并发，单次8000输出tokens，0重试；先无付费prepare核对实际数量。只移动整章唯一逐字引用的来源位置/跨度，不修改语义内容；旧明确负审核不重评，旧明确正审核按实际发送ID保留，漏项及此前因引用结构未送审项接受一次新审核。源代码和辅助函数复制到新的不可变目录运行，不覆盖旧产物。新输出`runs/supply-chain-learning-units-recovered-20260909-01`。

恢复已完成44调用，0调用失败、14个审核漏项继续隔离，最终253单元（218训练/35开发）；保留旧明确正评146，新增通过107。计量485317 tokens。原文找不到/多处歧义的引用拒绝，旧40负评不重评。下一步是跨章概念去重和任务生成，不再重复修同一批引用。

Agent环境采用登记的新同配置回归协议：新建`runs/supply-chain-agent-regression-env-20260909-01/sandbox`，仅复制原数据库及104份documents，排除sandbox顶层历次生成报告/图表/任务清单；复制前后验证源文件哈希一致。schema_dictionary.md由SQLite现有DDL只读导出，不含数据行或任务答案，所有模型共用。不是历史分数的精确复现；仅用于本轮Step120/CPT/候选的同条件配对保持检查。共享sandbox不修改。实际工具必须设置PI_AGENT_SANDBOX_LOWER为新根，不能只让预检指向新根而工具仍用旧根。总复制量约6.3MB，无新增API。

隔离环境已实际构建，105个源文件共6250741字节，源前后/复制哈希一致；现有pi_runtime_preflight已通过。尚未实际跑模型Agent回归。

紧接着启动任务数据批次，**不再等待下一次催促**：`build_grounded_supply_tasks.py`，一次全量概念语义去重+最多253单元×生成/独立审核两次，硬上限507调用、并发64、每次输出上限8000 tokens、0重试，最多1012题。精确同名及语义同概念合并，开发优先；去重只用授权单元名称/定义/条件，无评测输入。每单元最多四类：自由短答、单选辨析、多选条件判断、8–20候选概念匹配；不能支持则少出或拒绝。邻居只能同一split，采用已审过的概念定义而非复制20份完整教材；主单元原文证据保留。错误选项须能由定义/条件反驳，不能把来源未提及当错误。

新QA生成与审核仍只通过百炼。机械构造仅格式、索引校验与本地防重叠筛查，不在本地编写知识答案。官方内容仅在服务器本地防重叠索引使用，不发给API。任务候选仍须最终质量/去重/开发评分核查和训练协议冻结，不自动晋级。输出`runs/supply-chain-grounded-tasks-20260909-01`，不可变脚本/辅助函数在`/opt/llin-grounded-task-code-20260909-01`。独立预答和Agent/通用回归准备可在任务构造期间推进。

本批已完成44章/88调用，计量1,006,224 tokens（837,831输入+168,393输出），通过127单元（112训练、15开发）。8项结构/引用测试通过。保留原始响应，不把127单元称为训练完成。

生成522单元，193通过严格结构。6章审核返回缺少部分ID，按原整批规则隔离：7/11/13/14/26/44章；不是API超时或输出截断，全部finish_reason=stop。原不可变脚本最终status仍写units_ready，但failed_chapters=6、training_ready=false；仓库脚本已修正未来运行应写partial_units_need_recovery，未覆盖旧运行代码/结果。

重要工程发现：408条不匹配的引用中56条在本章另一个窗口唯一命中、190条是跨800字符窗口边界的原文、162条在本章根本找不到；空白归一化没有救回任何单元。**不能把这个拒绝率解释成教材知识不足**，它部分来自引用窗口格式设计。

下一步先无付费恢复原文位置：只对在整章唯一逐字命中的quote映射到真实字符区间/相邻窗口，建立可逆的原文跨度引用；不得改写quote来凑匹配，找不到原文的仍隔离。不修改任何概念、定义、条件、公式等语义内容。现有审核仅针对当时实际发送的单元，不可给未审核候选继承通过。修复完成后重新登记一个最多44调用的来源复核批次，重新审查恢复单元；若沿用已有缺项审核，只保留ID唯一匹配且所有条件真、issues空的明确通过单元，缺项仍拒绝。之后再进入概念去重和任务组织，不再重复整章生成。该恢复批次本次**尚未调用**。

Agent回归核验取得实际进展：train236/val20/test20的instruction_sha256分别唯一且三者两两交集0；文件sha分别925c2db871cce77016319264c1555b1ddfdfbe8159c6f968f840717e09507ec0、0fa49b2ba2a1ea662887017b96c65de9142bc39af8aad74774817cfc0373c08c、dd72b5f7ef1f55ea43f51c3b0b47abb6a15f46afc0c39509c994d92c74dcd1c1。最初误读不存在的extra_info.task_id导致空值集合交集1，已纠正为真实instruction_sha256，不能把那次误查当泄露证据。

`/pi_sandbox/sft/20260628_v15`及数据库/documents存在，但缺schema_dictionary.md；原宿主路径也缺该文件，因此新`pi_runtime_preflight`不通过。不要篡改检查或悄悄向共享sandbox加文件。下一步核对历史协议是否本来由SQLite schema查询获得结构、是否有匹配的干净原始sandbox快照；若需隔离副本和显式schema导出，则先登记这是新的同配置回归条件，并对Step120/CPT/候选一致使用，不直接声称复现旧历史分数。当前缺口仍有安全核查路径，尚不需要用户介入。

通用测试文件已确认宿主MATH-500/default__test.jsonl、GSM8K/main__test.jsonl；原始目录未挂入容器。后续通过明确文件复制到私有评测路径并冻结hash，不外发，不进入训练；沿用opensource_reward.py评分入口并先核验题源隔离和正反例评分。完整通用回归范围/预算尚未冻结，不能把只看数学两套称为全通用能力覆盖。

### 本轮实际推进与冻结协议（2026-09-09，覆盖上述历史待办）

引用恢复已完成：44次调用、485317 tokens，得到253单元（218训练、35开发）。概念去重保留243单元，任务生成/独立审核487次调用、2792420 tokens，得到697题；0调用失败。安全汇总见`supply_chain_learning_units_recovered_20260909.safe.json`及`supply_chain_grounded_tasks_20260909.safe.json`。上文“尚未调用”“缺schema”描述是先前状态，不再是当前阻碍。

人工按ID哈希每类型抽查2条共8条教材QA，发现C39-U06-Q1把教材特定流程的“第一步”问成未限定框架的问题，隔离该条，未本地改写知识。其余抽查不能代表全量人工通过。全量结构、ID、完整输入去重、引用单元同split及章节隔离检查完成，无完整输入重复，无跨split引用。最终**591训练、105开发**，覆盖199/35个主知识单元，训练监督18579 tokens，总序列88819 tokens。训练含189短答、187单选、190多选、25匹配；开发含31短答及74客观题。保留章节已被此前CPT看过，不能称未见知识泛化。

每题只生成一个监督记录，不复制扩量。短答使用API原答案；选项题按ID哈希固定分配“仅JSON答案”或“API原简短解释+JSON答案”，各自提示要求与目标一致，所有选项零基索引。只监督assistant和EOS；不把教材、rubric、题面作为答案监督。开发31短答只做损失诊断，74客观题严格集合判分，不以漏评短答的结果冒充105题准确率。可复查转换脚本`prepare_grounded_supply_training.py`，相关11项测试通过。

冻结数据位于`runs/supply-chain-task-training-data-20260909-01`，本地安全清单`supply_chain_task_training_data_20260909.safe.json`含全部SHA。新任务训练前预答已完成：Step120训练客观题363/402、开发65/74；CPT为364/402、67/74。两起点已能答对大多数新题，因此本轮不预设SFT必有大幅收益。数据不根据这些预测再次筛选/改写。

**本轮训练锁定一臂：纯教材CPT step116 → 新任务SFT。** 不从旧355QA候选继续。全参BF16、16 NPU、TP4/PP2/CP2，fresh Adam，学习率2e-7保持上一低暴露试验设置；global batch8、单遍73步，实际584样本曝光，7条尾部由固定训练加载器丢弃，明确不是591条全部完成一次曝光。数据集总监督tokens不等于实际曝光tokens。只保存最终step73；开发loss诊断，不选官方分数最高checkpoint，不增加第二遍/调参网格。学习率低是控制遗忘的工程选择，不是已证实最优；本轮同时改变任务内容与监督形式，不能单独归因某一因素。Step120仅作基线，没有直接SFT对照臂，所以不能证明CPT前置优于直接SFT。

输出`/opt/llin-grounded-supply-sft-cpt-20260909-01`，脚本与关键helper复制到唯一不可变目录后执行，使用同一资源锁，剩余磁盘≥180GB，不覆盖/删除旧模型。出现非有限loss、加载/保存失败立即停止该流水线，不绕过检查重训。

**训练后验证协议已预定，不以结果修改：**

- 学习检查：同一402训练客观题和74开发客观题，Step120/CPT基线已跑；候选相同JSON提示、temperature0、seed1024、96输出token、4096上下文、TP8，单轮，分别报告两个分母。
- 正式领域：冻结原1672题、相同运行参数，各模型三轮；比较Step120、纯CPT、候选；报告SC226/Logistika1446、配对得失、全部失败计错、重复稳定性。单题波动不能算可靠收益。不用官方题做QA训练、改写或API复核。
- Agent：已冻结隔离环境与20道test；Step120/CPT/候选使用同一环境，3次采样/题（60轨迹/模型），TP8/DP1，沿用standalone的temperature1/top_p0.95/top_k20和相同上下文/工具限额；按任务聚类报告均值和配对变化，不把60条当60个独立任务。不是历史旧环境分数的复现。工具实际PI_AGENT_SANDBOX_LOWER必须指向隔离根，模型间独立run tag。模型及20题加载预检已通过，实际Agent推理尚待执行。
- 通用：已冻结314题（MATH-500 test100、GSM8K test100、MMLU test57学科各2），排除已知Step120训练重叠，按固定hash选样不看预测。文件SHA c040067274ef08692d3d8ef9a60f1db4041bb26d326c2c14b35f9871dde844cb；`runs/supply-chain-general-regression-data-20260909-01`。MMLU合并文件不重复采样，JSONL按真正换行解析。每模型一次temperature0、seed1024，禁thinking、输出上限2048/context4096；沿用严格最终答案评分器，预先验证正反例，截断/失败计错。仅覆盖数学/知识，不代表全部通用能力。
- 不把训练成功当交付达标：开发、官方收益和Agent/通用保持均须报告。任何分项退步都明示；不因小样本“无显著差异”宣称等价。默认不替换当前交付模型，证据不足则结论为未达到目标，不自动无限补数据/追加训练。

实际训练已启动，检查到rank8 **step20/73**、loss2.2680745、grad_norm27.3765，均有限。rank0不输出训练metric，不应仅查rank0误判未推进。mstx range_end签名告警在上一轮已完成训练也存在；本次保留原日志，未热补丁运行环境。继续检查训练/导出是否完成，不能把此告警单独当失败或把进程存在当训练成功。

通用314题预检通过，最大prompt544 tokens，评分器6个正反例通过。已启动独立自动衔接程序`/opt/llin-grounded-validation-code-20260909-01/run_grounded_supply_validation.py`，状态`runs/supply-chain-grounded-sft-validation-20260909-01/status.txt`当前waiting_for_training_export。只等待本次明确成功导出状态，最多8小时；失败停止；成功后用资源锁串行执行Step120/CPT/候选的官方三轮、通用单轮，以及候选的新任务学习检查。不是修改运行中的训练脚本。完成标记明确为domain_tasks_general_complete_agent_and_analysis_pending，**仍需实际Agent回归及最终配对分析**，不能见该状态就标全任务完成。下一次自动跟进优先检查这两条在途流水线；正常运行时继续准备Agent执行入口，不重复训练/制数、不等待用户催促。

### A. 完成来源能力清单与回归可用性核验

- 从分类私有结果中读取教材具体主题/限制，按完整章节或相邻块恢复上下文，登记可验证概念、对比关系、规则及适用条件；排除缺图/缺公式内容。不得按基准具体错题反查原书来编题。
- 基于这份独立来源清单组织任务：短问答保留为维护；重点增加相近概念辨析、按给定条件判断陈述、独立候选表匹配/多选完整性、可核算计算。候选和干扰项均由教材规则支持/反驳，不依赖生成模型凭空编造。
- 基础QA/规则单元与任务变体属于同一概念组，先划分再生成，开发概念组不进训练。旧71开发已经看过且评分有缺陷，只作探索性诊断，不伪装全新盲测。
- 同时定位原Step120回归数据身份、加载接口和sandbox服务。不要等训练后才发现无法回归。通用训练80题不能当通用评测；旧dense-correctness Step120不是本轮opensource Step120。

### B. 数据与训练方案冻结后执行

- 每个付费制数/审核批次先在此登记来源、最大调用数、输出上限、并发与拒绝规则；生成/审核只用5号机百炼qwen3.8-max。本地模型只预答/训练，不担任训练数据作者。
- 不预先硬凑2000–3000题，也不退回6题；按独立概念覆盖和可靠性组织成批数据。记录每种任务的有效概念数、重复数、答案长度、监督tokens。明确教材未覆盖的范围。
- 训练前冻结数据哈希、开发评分、起点、训练量、学习率、checkpoint策略、停止标准与所有回归协议。用户已授权合理训练，无需再询问常规开跑，但不能绕过安全或缺失资料。
- 优先检验有价值的新数据/任务设计；Step120直接SFT对照用于区分CPT前置价值，不以旧+2题认定CPT最优。如采用两起点必须同数据和预算；若一臂则明确其结论边界。此处尚未批准具体超参数，需依据分析选择并写入冻结协议。
- 学习检查是辅助：训练题是否学会、独立开发是否改善、官方两套是否提高、原能力是否保留。每项都要报告，不靠训练loss或1题波动宣称成功。失败保留，不自动追加无上限训练。

### C. 完整验证与交付

冻结最终候选后，运行同配置三轮官方知识评测、原Agent/通用回归，报告各数据集配对得失、波动、解析失败、成本与局限。明确实际收益是否达到目标，不偷换成“任务跑通”。保存安全证据，更新README、提交推送origin/main，然后通知用户。硬性外部阻碍则先报告具体需要，不假装已完成。

## 环境与恢复入口

- SSH：`huawei-05`；容器`llin-verl-trainer-m05-20260730`。
- 容器仓库`/workspace/llin-verl-grpo`；宿主机仓库`/data3/llin/qwen3.6-27b-verl-grpo`。
- 覆盖运行：`runs/supply-chain-global-coverage-20260909-01`，已完成；`classification.private.jsonl`含来源分类和API响应，禁止提交。`coverage_join.safe.json`为无正文汇总。
- 覆盖不可变主脚本：`/opt/llin-global-coverage-code-20260909-01/audit_supply_chain_coverage.py`。调用的仓库API辅助函数本轮未更改；后续长任务应一起固定依赖快照和hash。
- API配置：`private/chat_api_config.user.json`，只读取使用，绝不打印凭据。
- 纯教材CPT：`runs/logistics-cpt-book-exposure-curve-2x4x-20260904-01/hf_export_step_116`；native `checkpoints/global_step_116/model/dist_ckpt`。
- Step120：`runs/llin-step120-opensource-20260825-02/hf_export_step120_opensource`；native `checkpoints/global_step_120/actor/model/dist_ckpt/.metadata`确实存在。
- 旧355QA SFT：`/opt/llin-capability-sft-cpt-20260909-01/hf_export`。新训练不要从这轮SFT继续作为默认。
- 同配置补测：`runs/book-capability-sft-aligned-20260909-01`，不要依据failed标记重跑；完整性已读检验，后续可生成单独恢复核验文件而保留旧事故日志。
- 历史Step120安全基线：`runs/logistics-cpt-diagnostics-20260904/safe/public_eval/step120.majority.safe.json`。
- 通用原始数据宿主根路径`/data/renjunxiang/coding/huawei_train/datasets/open_source/raw`已只读确认存在，含MATH-500、AIME24/25、BBH、C-Eval、CMMLU、GSM8K、MMLU-Pro等目录。需核验文件身份、split、原训练排除清单及可用评分器；不直接拿所有目录当冻结回归集。
- 原Agent候选：`data/boss_v15_dwh_full276_20260806/dataset`已确认含`boss_pi_train.parquet`、`boss_pi_val.parquet`、`boss_pi_test.parquet`及`boss_alignment_contract.json`。核实隔离、sandbox版本及本轮Step120加载后才能采用；candidate128默认dense Step120分支不可直接套用。
- 项目盘曾仅54GB余量，大模型输出使用容器/opt；/opt不是持久项目绑定卷，删容器前需备份。绝不删除旧模型释放空间来暗中推进。
- 后台自动跟进`automation-5`已激活，每30分钟继续本任务；不是只监视状态，应在没有在途任务时实施下一步。完成/需用户介入时暂停。未更改另一个已暂停的旧Step120自动任务。

## 公开来源与限制

只使用[LogistikaBench官方数据卡](https://huggingface.co/datasets/berdymurad/LogistikaBench)和[SC-bench官方仓库](https://github.com/Damon-GSY/SC-bench)的任务说明核对范围；前者网页自动附带题目预览，未将这些预览纳入分类API、训练或开发制数。后续读数据卡应优先只取README，避免自动题目预览。SC工具编排不是这226知识题成绩的含义。

自动跟进按[官方计划任务文档](https://learn.chatgpt.com/docs/automations?surface=app)及实际工具配置，工作依赖本机/服务可用性，不保证离线时持续执行。

数据质量技能促使本轮分开匿名错误分布、AI主题标签和真实来源覆盖，不把任一代理量当成因果结论。[核算notebook](supply_chain_global_coverage_20260909.ipynb)验证完整分母、分类数量和错误占比。分类器6项结构测试通过。
