# CPT/SFT自主假设检验：双评测提升至少3个百分点

2026-09-12，用户授权使用已有材料和机器，自主设计、训练和验证，持续推进直到达成目标。旧实验的停止追加训练决定不再约束这项新实验；保留旧产物，不恢复旧协调器或重复已完成实验。2026-09-14最新状态：S1/S2/S3均结束且未达标，正在审查S4操作任务来源覆盖；以文末最新记录为准。

## 成功标准

默认将“3%以上”解释为两个评测集分别至少增加3个百分点，比相对提高3%更严格。固定原始Step120基线，SC-bench知识189/226，LogistikaBench1180/1446；整数门槛分别为**196/226**、**1224/1446**。不能用合计上涨抵消其中一个数据集不达标。

原评测题、标签、提示、上下文/生成预算、评分规则不修改。保持thinking关闭、temperature0、seed1024、输出96、上下文8192、TP8、每题三轮严格多数票；无效和截断计错。候选冻结后另起新进程复测，并独立报告通用/Agent保持能力。三次确定性重复不是三个独立训练种子。由于两个基准已反复用于实验选择，结果必须称为适配/选择集收益，不冒充独立泛化测试。

## 复查后仍成立的事实

| 已有实验 | 可支持的结论 | 不能支持的结论 |
| --- | --- | --- |
| 116块原教材8遍CPT曲线 | 该数据与5e-7配方内净收益很小 | 所有CPT数据组织和知识暴露量都无效 |
| 全13份材料加205条知识，单遍CPT | 净增5题；新增知识23,131/1,868,012 token，约1.24% | 针对性知识已被充分学习 |
| 591条任务型SFT | CPT起点、2e-7、1遍；训练客观题364→369/402，开发67→65/74 | 直接Step120起点、更充分学习或不同覆盖都无效 |
| 官方原题同题SFT | 可大幅提高训练过的题；说明系统并非完全无法更新 | 可作为本次独立能力目标的达标证据 |

来源：[整体复审](supply_chain_rethink_20260909.md)、[旧任务数据](supply_chain_task_training_data_20260909.safe.json)、[最新CPT结果](cpt_knowledge_complete_result_20260911.safe.json)、[同题SFT限制](logistics_mcq_fit_results_20260907.md)。旧591条训练在batch8下明确丢弃7条尾样本，这是既有记录，不是本轮发现的隐蔽故障，不能据此解释全部差距。

## 第一组可执行对照

S1/S2都使用原始Step120模型态、新优化器、同一591条来源审核任务，固定数据SHA `2f10b42c9a8ad0bde49b2c1887a6216727352c4871d1afdc5e84145e70dd9051`。新两臂采用全局batch3、197步一遍，完整覆盖591条；必须先通过实际并行训练环境的兼容性和监督预算检查，不能只凭整除关系宣称可运行。

- **S1：2e-7；S2：1e-6。** 同起点、同数据顺序、同步数，仅学习率不同。两者互相比较可检验该数据上的更新强度影响；相对旧CPT起点实验还改变batch/尾部覆盖，不把旧新差异当纯起点效应。
- 训练前分别测训练客观题和开发题；训练后同时检查训练损失、客观正确率、两基准得失和保持能力。不能只看loss下降判定学习成功。
- 若训练/开发均改善，再冻结下一轮暴露量对照；若训练改善而开发不改善，优先改善覆盖和迁移；若只有似然改善而自由作答不改善，检查答案监督内容与条件辨析，不盲目追加遍数。

这些是待检验假设，不是已经找到的原因。第一组失败不等于整体目标失败，也不自动转向无限学习率搜索。

## 并行准备的数据方向（不使用子代理）

205条核对知识已转换为205份本地制题请求，按共享证据记录连接成115组，暂分178个训练单元、27个开发单元。要求每单元尝试定义辨析、边界/反例、新实体或数字的应用，不能支持的题型明确跳过。**目前生成题数为0，训练就绪题数为0**；制题请求不是已审核QA。

同证据记录整组切分只是初步防泄漏，随后还需跨来源的概念/语义去重和来源支持审核。开发单元的知识可能已经在基础模型或旧教材CPT中出现，不声称知识首次曝光。用户提供的13份材料继续可用，不因评测同源排除整个来源；新SFT不直接使用官方题面、选项及金答案。同题派生重写不能伪装成独立训练数据。

后续候选S3检验“更相关的条件与任务覆盖”；C1检验“针对性知识暴露被整书训练稀释”。C1的暴露和背景token预算在知识探针之后冻结，不先拍定更多重复一定有效。新增知识、回放/保持数据需分别记账，所有失败点保留。

研究参考：[KEFT，TACL 2025](https://aclanthology.org/2025.tacl-1.49/)研究从领域语料生成QA和知识相关训练目标；[Gradual Learning](https://arxiv.org/abs/2410.05802)研究部分掌握知识的选择。这些支持设置可检验的数据/学习假设，不能保证本项目收益，也不能由论文推断LoRA天然防遗忘。

## 实际阻碍与恢复步骤

2026-09-12实测：huawei-05、huawei-06、huawei-00均在SSH版本握手阶段被重置。调试确认TCP已建立，但尚未收到服务器SSH版本、未进入密钥认证；不是已观察到的密码或私钥拒绝。未修改网络、密钥、服务器服务或终止任何任务。已向用户询问访问网络/地址是否变化，暂无回复。新的实验尚未运行。

恢复连接后：

1. 核实机器身份、现有训练占用、真实挂载和剩余磁盘；新模型/检查点使用`llin`前缀与独立目录，禁止覆盖旧任务。
2. 核验现有591任务、105开发任务和原Step120文件；运行实际分词、答案掩码、完整采样及预算门禁，并准备训练前探针。
3. 完成S1/S2专用脚本和小型兼容性验证，依次训练、导出、固定双评测及任务/通用回归。不能直接运行旧`run_grounded_supply_sft_m05.sh`，它固定CPT起点、batch8和旧输出路径。
4. 根据结果继续S3或C1，更新不可变实验注册记录；保留失败实验、无效/截断及退步题，不只挑最好轮次。
5. 达标后独立复核、保存模型与完整结果，更新README、提交并推送，再暂停自动跟进。未达标不能把目标标为完成。

## 本轮交付

- `scripts/prepare_cpt_sft_search.py`：冻结整数达标规则、初始对照和来源制题请求，构建器不读取基准题/答案。
- `tests/test_cpt_sft_search.py`：5项测试覆盖整数阈值、双基准同时达标、基线不可替换、重复数据集拒绝及独立确认要求。
- 私有准备：`CPT_resources/llin-cpt-sft-search-20260912/`；来源文本不入Git。
- [初始实验注册](cpt_sft_search_plan_20260912.safe.json)。目标处于进行中；网络障碍尚未排除。

### 后续数据预检准备（2026-09-12）

上轮完成注册和来源请求准备，归类为实质进展。本轮再次连接huawei-05，仍在SSH版本握手阶段重置；没有确认任何新训练进程存活，不归类为等待训练完成。

新增 `scripts/audit_cpt_sft_search_data.py`，在恢复后用训练容器内原Step120模型和现有任务目录执行。工具核对train/dev Parquet及tokenizer身份，按实际模型模板重建每条输入和答案，逐条验证监督边界、EOS和loss token计数；校验591/105条完整batch3预算及跨split ID互斥。输出明确标注分布式加载器尚未验证，不凭此自动启动训练。工具只读取本地模型和数据，不输出题文或答案。

定向测试共9项通过，其中新增4项覆盖预算、错误边界/目标token/EOS/类型、尾批丢弃与重复消息拒绝。真实容器预检、PP2下batch3兼容性、真实采样覆盖及新实验训练仍未完成。

## 2026-09-14：以最快获得实验结论为优先

更新执行顺序：先S2，保留S1为后续需要时的同数据学习率对照，不先运行整书CPT或等待新题库。两臂注册条件不变；优先级在看到S2结果前调整。该选择检验更新强度不足的假设，不承诺一定达标。新制题仍可作为后续覆盖实验，不将现有205份请求称为已审核任务。

- huawei-05 SSH已恢复，机器身份与旧记录一致；NPU检查未发现运行中的计算任务，设备健康OK。容器`llin-verl-trainer-m05-20260730`仍在，/opt可用约1.6TB，采用该位置保留新检查点与导出，不删除旧产物。
- 真实模型分词与监督审计通过：train591条、88,819序列token、18,579监督token；dev105条、16,345序列token、3,295监督token，SHA与注册一致。
- `gate_cpt_sft_search_loader.py`实际调用容器安装版`SFTTrainer._build_dataloader`，使用DP1引擎接口替身和真实数据集/整理器；逐条核验采样索引、input_ids、loss_mask、position_ids，训练197批及开发35批全部通过。这证明加载路径，不等于已经验证完整分布式模型训练；PP2兼容性和实际训练预算仍以本轮运行结果核实。
- 不可变代码快照：`/opt/llin-sft-search-code-20260914-01/scripts`。独立输出：`/opt/llin-sft-search-s2-20260914-01`。协调器容器PID **3204924**，已用实时ps核实命令及存活，首次观察约2分钟时状态`step120_tasks`，8个推理worker正在加载原Step120。
- 协调器`run_cpt_sft_search_s2.py`持有全程资源锁，先对402训练客观题+74开发客观题做一次探针，随后启动`run_cpt_sft_search_s2.sh`完成197步SFT；保存完整model/optimizer/extra，导出到`llin-training/llin-step120-s2-hf`；核查197步和88,819实际训练token，再测同任务和固定两套各三轮官方评测。只输出候选结果，不自动晋级；通用/Agent保持与冻结后独立复核仍待做。
- 启动器bash语法校验通过，新增Python文件语法校验通过，原9项定向测试通过，真实加载器门禁通过。不得把这些检查称为完整训练或评测通过。
- 自动跟进`cpt`已更新为确切新进程及路径，每30分钟检查，不再按网络未恢复或旧实验待启动处理。断连/观察超时不等于任务失败，不得重启已活跃的协调器。失败时保留快照与日志，查明后使用新版本恢复，不原地改动运行中代码。

下一次检查本轮status、子进程和日志；若失败先处理实际错误；候选完成后分析训练/开发得失及两评测逐题对比，再决定是否执行S1或改进数据覆盖。初始JSON注册文件保留为准备时快照，其`training_started=false`不是实时任务状态。

### S2训练完成，恢复评测（2026-09-14）

用户询问进度时核实：197/197步完成，末步loss约0.2653、LR1e-6；模型导出检查通过，1199张量、15分片，无缺失、多余、形状或dtype不一致。协调器随后在导入`summarize_logistics_cpt_run`依赖的`scripts.verify_checkpoint_integrity`时失败，原PID3204924已不存在。原因是只给子进程设置了快照根目录PYTHONPATH，协调器自身没有；并非训练或导出失败。

修复`run_cpt_sft_search_s2.py`在自身sys.path优先加入快照包根目录，新增`--after-training`：要求原协调器明确失败及导出索引存在，校验数据身份，跳过原模型探针、训练与导出，只接续训练审计、新模型探针及双评测。恢复状态单独记录`status_recovery.safe.json`，原失败状态、日志及快照保持不变。禁止已有恢复状态时重复启动。

真实容器验证：依赖来自新快照`/opt/llin-sft-search-code-20260914-02/scripts/verify_checkpoint_integrity.py`；全量日志解析197个唯一训练步，总token88,819。原9项定向测试通过。恢复容器PID **3224414**，实际命令带`--after-training`，已核实存活；首次状态`data_audit_recovery`。下一次应优先检查此恢复进程和恢复状态，不读取旧failed状态后再重训。

训练前任务探针：训练363/402、开发65/74，合计428/476；这是本轮原Step120的固定设置结果，不能用旧CPT起点364/402、67/74替代。新的候选任务/双评测结果尚未完成，目标未达成。

### S2结果与S1启动（2026-09-14 11:00后）

S2恢复协调器已结束，终态`candidate_evaluated_review_pending`，PID3224414不存在。训练题363→393/402，开发65→68/74；SC189→186/226（改对6、改错9），Logistika1180→1175/1446（改对31、改错36）。目标未达成，不晋级模型，通用/Agent保持未验证。开发仅74道客观题，一次探针涨3题不支持强泛化结论。

`verify_sft_search_result.py`在服务器独立重解析基线与候选共10032条原始输出，核对冻结题目SHA、逐题标签、3轮身份、多数票及协议所有非模型字段；两侧各5016请求、无无效或截断。安全结果见`cpt_sft_s2_result_20260914.safe.json`。本轮一度SSH重置，复查恢复后完成审计；未因此重启旧任务。

下一步执行已注册S1，严格保持同591条任务、原Step120、新Adam、batch3和197步，仅LR降为2e-7。执行器增加`--arm S1|S2`，启动器只允许这两组学习率并使用对应llin模型名。S2快照保持不变。服务器再次检查NPU无运行计算进程，/opt约1.2TB可用。

S1代码快照`/opt/llin-sft-search-code-20260914-03/scripts`，输出`/opt/llin-sft-search-s1-20260914-01`，容器PID **3232567**（已核实存活及`--arm S1`命令），首个状态`data_audit`。先全量门禁和476题原模型探针，再训练/导出/任务及固定双评测。旧S2原failed与恢复完成记录不代表此S1状态。9项定向测试及新Python/启动器语法检查通过。

比较S1/S2前还需核对两次运行真实采样顺序、实际token预算和原模型探针。S2证明这批来源任务可被更充分学习，尚未证明目标评测受益，也不能唯一归因为学习率或语料错误。若S1同样无收益，优先知识条件/任务覆盖方向，避免仅增加这批简单任务的遍数。

### S1完成，转入来源任务覆盖（2026-09-14 11:36后）

S1终态`candidate_evaluated_review_pending`，PID3232567已不存在。197步/88819token审计通过；训练客观题363→373/402、开发65→65/74，SC189→190/226（改对1、改错0），Logistika1180→1169/1446（改对8、改错19）。独立重解析基线及候选10032回答通过，协议一致、零无效/截断，结果存`cpt_sft_s1_result_20260914.safe.json`。两臂实际loader报告（含顺序摘要）完全一致、197步逐步token序列完全一致、原模型任务探针汇总一致。这些支持LR对照；未复核训练探针每题预测完全一致，不将三次确定性推理称为独立种子。

两臂都未达到目标，不晋级，不追加相同简单任务的训练遍数。S2更强的来源任务学习没有对应目标收益，S1降低更新强度也未解决Logistika退步；下一步优先检验知识条件和任务覆盖，尚不能唯一归因于语料或遗忘。

新增`generate_source_condition_tasks.py`，使用原205份已核对来源请求，SHA固定；输入只有标题、范围、知识正文及来源摘录，不包含官方题目答案。共享证据与topics联合成29个连通组，按组确定开发/训练分配，比原仅证据分组更严格，但仍不能取代跨主题语义审查。每单元最多生成3种闭卷客观任务：定义辨析、边界/反例、新实体情境；不支持的形式跳过。生成后先检查结构、选项、答案范围和逐字来源引文，再用同一模型做不显示拟定答案/解释的盲解题审核。

只使用本地原Step120推理，TP8、8192上下文、输出2048、生成seed2048温度0.4、审核seed2049温度0。保留原始输出与所有拒绝统计。通过筛选的任务仍明确`training_ready=false`，同模型盲审不是独立事实认证；需要人工语义重复/来源支持审核、难度及训练前探针后才决定S3训练数据。全部13来源仍可用，不用原题重写凑效果。

5项本地结构检查（传递分组、合法任务、伪造引用、全选答案、重复选项）通过。检查NPU空闲和/opt约815GB可用后启动来源扩充进程：代码与请求`/opt/llin-source-expansion-code-20260914-01`，输出`/opt/llin-source-expansion-20260914-01`，容器PID **3252773**，命令`generate_source_condition_tasks.py`。实际ps已核实存活，首次状态`preparing`；尚无生成题数或新训练结论。先检查此任务及其子进程，不恢复已完成的S1/S2训练。

### 来源任务逐题审阅及难度探针（2026-09-14 12:47后）

制题PID3252773已结束，终态`generation_review_complete_manual_audit_pending`。205来源单元产生196结构合格候选，194通过同模型盲审，另有118条生成任务未通过结构/逐字引用等检查、2条未通过盲审/重复检查。过滤通过的194条包含116定义辨析、57边界、21应用；并不等于足够难或全部正确。

本次直接阅读全部194条的来源、题面、选项及答案，发现同模型盲审漏过多项错误：把满足较高速度的线路误排除于最低速度阈值；将车辆设计用途与当次用途混淆；忽略30天伤亡统计边界；将同国两地途经第三国运输误排除于过境；把“可能分隔”改成“通常分隔”等。对答案不唯一、条件不足、铁路范围套用于道路情境或同条件重复等共37条暂缓，不篡改官方评测标签。其余157条保留作难度探针，仍不自动开训；这是Codex来源审阅，不冒充人类专家签字。

对保留题修整“provided definition”等悬空表述，并在统计术语题明确使用transport-statistics glossary口径；固定水位运河条件和教学融资模型范围补齐；清理一个选项自带字母标签。原答案位置97/194落在0，仅2条单选答案落在3，因此在新来源题上做确定性位置平衡/打乱，并验证正确选项内容在置换前后不变。官方评测未修改。

私有审核目录（本地）`CPT_resources/llin-source-reviewed-20260914-01`，逐条决策`decisions.private.jsonl`、整理题`reviewed_tasks.private.jsonl`、闭卷探针`cases.private.jsonl`；输入194条摘要SHA `cb8085d17b6b0573964540b72d6e3a3991cd617df114ba271857161926f271eb`。最终157条：训练135、开发22；定义101、边界39、应用17，来源主题组互斥。探针SHA `d896fd906a17c473d4ec9ef775f1ad0ed7138d9fbec431fec5efcab67a3856bd`。安全摘要`docs/cpt_source_task_review_20260914.safe.json`。

已上传到容器`/opt/llin-source-reviewed-20260914-01`，启动原Step120闭卷探针，TP8/8192上下文/96输出/温度0/seed1024/1轮，标签`step120-source-conditions-probe`，使用全程资源锁。最初容器PID **3258744** 是启动包装进程，须再核实其exec后的flock及实际Python子进程，不能仅看PID或文件判断存活。输出`probe.private.jsonl`与`probe.safe.json`，日志`probe.log`。先看原模型已掌握多少，再冻结S3配方；若绝大部分已会，不能把这些简单定义题硬当主要提升方案。新训练尚未启动，3个百分点目标仍未达成。

### 难度探针已完成（2026-09-14）

实际Python PID3259082已结束。原Step120闭卷结果144/157（91.72%）；训练123/135、开发21/22。取回全部157个原始回答，逐题核对输入问题、选项、金标签与已冻结来源探针，重新解析JSON并重算评分，通过。安全结果`cpt_source_probe_20260914.safe.json`，非官方评测结果。

13道错误再次对照原始来源审阅：12道训练、1道开发；集中在运输统计的分类例外/范围（共享牵引转向架计数、非机械冷藏车分类、滚装客船排除、碰撞优先于脱轨、海船排除水翼/气垫、旅客数量阈值、单独列示的自航液货驳、跨国管道总长阈值、铁路渡运不算线路）以及搬运范围。唯一开发错误是来回两程的吨公里运力计算。它们不是自动盲审分数的重复宣称；已实际阅读来源和预测，但单轮探针不证明所有错误稳定。

下一步：不把大多数已会的简单定义题直接堆成新主训练集。优先从训练组内的已核实分类条件组合成更有区分度的来源任务/解释，保留原分组与22题开发冻结，不将开发错题改写加入训练。冻结S3的知识条件曝光与保持任务预算后再启动训练，按同一官方协议检验两套目标；不能凭新来源小测改善宣称达标。当前没有新的活跃训练或制题任务，S1/S2与本次制题和探针均已结束；后续自动跟进应实际推进S3材料和训练设计，不轮询已结束PID。


## 2026-09-14 S3 已启动

协调器 PID 3264229，huawei-05 / llin-verl-trainer-m05-20260730；快照 `/opt/llin-sft-search-code-20260914-04/scripts`，输出 `/opt/llin-sft-search-s3-20260914-01`。实测状态 training，已完成 data_audit、loader_gate 与 212 题原 Step120 探针；首次核对还在模型/优化器初始化，尚无训练后收益结论。

冻结数据 `/opt/llin-s3-data-20260914-01`：135 个已审阅训练任务各一次、11 个来源条件模板的 44 个变体各 6 次，共 399 记录、179 个不同任务。条件曝光交替短条件解释加 JSON 与仅 JSON，各三次。输入 72014 token、监督 6896 token，batch3、133 步、LR5e-7、原 Step120 模型及全新 Adam。开发 33 条包含原冻结来源组开发 22 条和同模板未见参数 11 条；后者不能算独立泛化，135 条基础题也不能替代通用/Agent 保持测验。私有题文、来源与审核记录不入 Git。

44 个变体不是 44 个独立知识点，本轮数据/曝光/LR 均变，是配方假设检验。注册文件的 training_started=false 表示数据构建时快照，实时状态以服务器协调器状态为准。自动接续 checkpoint global_step_133、llin-step120-s3-hf 导出、实际 token 审计、训练后 212 题探针与固定官方双评测。9 项既有测试通过，远端 shell 语法及实际数据/加载门禁通过。自动跟进已更新，不再重复旧 S1/S2 或制题/探针进程；目标仍未达成。


## 2026-09-14 14:13后：S3结果复核及S4来源审查

S3协调器3264229已退出，status=candidate_evaluated_review_pending。真实训练133步、399记录、72014输入token审计通过并导出。官方基线与候选共10032回答全部重新解析及严格多数计分；协议除模型路径外一致。候选有1条无效、0条截断，按错误计分。SC189→191（改对3、改错1），Logistika1180→1179（改对16、改错17），未晋级、未做通用/Agent保持。安全结果见cpt_sft_s3_result_20260914.safe.json。

212题来源探针逐题核对问题/选项/标签与冻结输入并重新解析：训练条件11→43/44、基础训练123→130/135、同模板未见参数2→10/11、来源组开发21→21/22。局部学习和同模板迁移成立，不能推断广泛能力提高。官方Logistika分类：搬运571→574/696、仓储151→153/202、运输333→326/404。S3偏重运输统计边界，不能据此认定覆盖全部运输能力；评测类别与细知识主题也不是一一对应。

下一步S4先做操作知识覆盖诊断，不增加S3曝光。已从既有13教材清洗块按操作主题关键词及600–5000字符筛出候选段落；私有目录CPT_resources/llin-s4-source-screen-20260914-01，安全统计cpt_s4_source_screen_20260914.safe.json。这只是候选索引，需排除习题/答案键/官方近似题、读上下文与公式完整性、映射冻结开发主题后才能制题；不能当作训练就绪。优先多种实际决策与计算任务，来源审核及原模型难度探针后才冻结新配方。未启动S4训练，所有旧进程均不重复启动。/opt剩余约439.8GB，低于当前完整检查点流水线550GB门槛；下一次训练前核实其他挂载可用空间或选保留产物的存储方案，不删旧产物。


## 2026-09-14 用户授权开始 LoRA-CPT

优先于S4制题，目标/官方协议不变。计划原Step120模型态、新LoRA及Adam，复用全参数CPT同一语料/workspace/llin-verl-grpo/runs/llin-knowledge-complete-20260911/train.parquet，SHA6ffa16684e617f657293918bf1d89ae8930ec24e0e08da346f175c4710c1f1c8。实际tokenization gate通过4912记录、1868012监督token、1872924输入token，无chat template、所有真实next-token目标监督。正式预算batch8、614步1遍。LoRA rank64、alpha128、dropout0、linear_qkv/proj/fc1/fc2；LR1e-5→1e-6为起始候选，尚未据有效学习校准，不能声称全参与LoRA只改变一个因素。

当前是两步工程门禁，尚未正式训练。huawei-05容器llin-verl-trainer-m05-20260730，最新flock PID3291008，代码/opt/llin-lora-cpt-code-20260914-03/scripts，输出/opt/llin-lora-cpt-gate-20260914-03，bootstrap在代码目录。01失败于未先初始化mindspeed导致缺TransformerEngine，02失败于Hydra已有model.lora字段不能单+新增；两次均训练前退出，保留快照。旧PID3288031/3289477的Z状态只是僵尸，不代表运行。

固定Bridge缺少新版引擎调用的create_peft/create_peft_hook等接口，采用进程内兼容：原生LoRA注入前调用既有Step120分布式加载，注入后跳过重复加载且断言前置加载发生；DDP/optimizer构建前基础权重冻结。所有参数逐rank训练前后哈希，要求基础不变且适配器变化；本次并未更改共享安装。新增export_lora_cpt_to_hf.py重建base+adapter并核验合并数学及HF完整性，尚待实机证明，不能仅凭语法检查启动正式训练。两步门禁与正式输出必须分开，正式训练应从新adapter重置开始；通过保存/导出/推理门禁后自动接续一遍双测。Python语法检查通过，正式流程及收益未验证。自动跟进已切换LoRA，保留S4候选但不同时占用机器。


LoRA门禁继续修复：03在原生LoRA注入时不识别MindSpeed TERowParallelLinear，04进一步暴露融合归一化列层未识别，均未进入optimizer/训练。最新05快照/opt/llin-lora-cpt-code-20260914-05，输出/opt/llin-lora-cpt-gate-20260914-05，flock PID3297018。新增进程内lora_cpt_ascend_compat.py明确映射分片属性；融合层对adapter重算相同归一化，避免把未归一化输入误送适配器。目标限定language_model.*的四类线性层，视觉分支/MTP保持冻结，哈希审计拒绝视觉adapter。导出脚本使用同一映射，尚未实测；正式训练仍未启动。此前所有失败目录保留。


### LoRA两步真实训练/保存门禁已通过（2026-09-14 15:00后）

05首次实际反向失败于冻结base参数main_grad=None而融合梯度累积仍访问其dtype；06关闭gradient_accumulation_fusion，保持基础冻结。06门禁PID3312417已退出，/opt/llin-lora-cpt-gate-20260914-06完成2步、5808输入token，global_step_2检查点保存，16rank before/after审计全通过（基础SHA一致、adapter SHA变化），并验证指标有限。不是正式训练或收益证明。其他控制沿用前述数据、rank64/alpha128、语言层投影、TP4PP2CP2。

自动接续协调器PID3331241，快照/opt/llin-lora-cpt-code-20260914-07/scripts，输出/opt/llin-lora-cpt-r64-20260914-01；首次检查状态gate_export，已独立读取2步指标与全部rank审计。接续CPU重建基础+适配器、逐模块合并数学检查、1199张量HF完整性、8道来源题推理烟测；全部通过才启动全新adapter/Adam的614步正式训练，随后同样审计基础冻结/adapter更新和实际1872924token，导出llin-step120-lora-cpt-r64-1epoch-hf，官方双评测及原始评分复核。两步模型不用于正式成绩。LR1e-5→1e-6冻结为首个LoRA候选而非已优化LR，保持原13步warmup；不能称已做超参数校准或单因素归因。注册快照training_started=false仅表示注册时状态。若某阶段失败，保留目录，从失败点修复恢复，不重复已完成训练。


导出恢复记录：协调器07/输出r64-01在适配器state额外状态键与named_parameters不一致处失败；08/输出r64-02已通过键完整性及加载，原生LoRAMerge.transform在BF16先舍入增量，未通过FP32合并数学断言。保持严格检查，改用原生merge函数的FP32输入，完成后一次舍入BF16；不用放宽断言绕过错误。最新协调器PID3336086，代码/opt/llin-lora-cpt-code-20260914-09/scripts，输出/opt/llin-lora-cpt-r64-20260914-03，正在gate_export。此前两步门禁06已完成不重训，所有失败导出目录保留。正式训练仍受合并/推理门禁保护，未确认启动。


09/输出r64-03已完成gate_export：160适配模块合并数学核验通过，HF1199张量/15分片，形状类型一致；报告cpt_lora_gate_20260914.safe.json。实际覆盖64层MLP的两种投影及16个全注意力层的QKV/输出投影；混合模型的线性注意力专用投影未在本配方目标中，不能称全部线性层LoRA。协调器3336086已进入gate_inference，正式训练仍等待8题推理烟测通过。S4继续暂缓。


## 2026-09-14 15:45 LoRA正式初始化失败修复

r64-03协调器3336086已退出。正式训练rank9在2026-09-14 07:12 UTC报Invalid device ID：推理遗留ASCEND_RT_VISIBLE_DEVICES=0..7限制了16rank训练，模型尚未初始化，weight_audit为0、无检查点、无完成训练步。此前“正式已启动”仅指流程进入该阶段，未证明训练正常；现在明确纠正。与语料/LoRA能力无关。

修复按stage生成独立环境，推理配置不再写回共享env；formal_training清除三种设备可见性变量，shell也清除。新增2项环境切换回归测试；实际容器用真实evaluation_env验证8卡推理到16rank训练可见性清理及父环境不变，shell语法通过。恢复入口--passed-gate要求旧状态failed且无模型初始化/检查点，并检查已通过的门禁、导出报告和8条原始烟测再复用。

新协调器PID3344468，快照/opt/llin-lora-cpt-code-20260914-10/scripts，输出/opt/llin-lora-cpt-r64-20260914-04，命令带--passed-gate /opt/llin-lora-cpt-r64-20260914-03。已进入formal_training；继续核实16rank完成初始化、实际训练指标后才称正常。原614步/4912记录/1872924token与LoRA配方不变，完成后自动导出双评测；不重跑两步或导出门禁，不删除旧结果。


## 2026-09-14 18:23 LoRA-CPT完成，目标未达

协调器3344468已退出，输出/opt/llin-lora-cpt-r64-20260914-04状态candidate_evaluated_review_pending。614步、4912记录、1872924真实输入token，16rank训练前后基础SHA一致、adapter均改变，全部loss/grad/lr有限；FP32合并160模块，HF1199张量/15分片完整。正式模型llin-step120-lora-cpt-r64-1epoch-hf保留未晋级。

再次独立运行verify_sft_search_result.py重解析基线/候选共10032回答并重算三轮严格多数，协议除模型外一致，零无效/截断。SC189→190（改对3、改错2，+0.4425pp）；Logistika1180→1184（改对21、改错17，+0.2766pp）。门槛196/1224均未到。此前同语料全参数CPT为SC189（改对2/改错2）、Log1185（改对12/改错7），此次LoRA并未减少Log上的改错数量，净收益也无明显优势；LoRA和LR均不同，不能据此断言所有LoRA都较差或冻结基础就不会遗忘行为。保持能力尚未单测。安全结果cpt_lora_result_20260914.safe.json。

已接续对最终LoRA模型做冻结212题来源探针（/opt/llin-s3-data-20260914-01/cases.private.jsonl），目录r64-04/source_probe，单轮相同8192/96/TP8协议，不训练不改变官方评分。与原Step120在S3前测的157/212及分组结果比较，先判断正文知识是否学到及不同任务的迁移；不将同模板检查当独立泛化。探针结束后按证据决定最小下一实验，原LoRA正式614步和双评测不能重复启动。


## 2026-09-15 LoRA来源探针完成

服务器source_probe/verified.safe.json已核实，PID3406383为Zs，任务结束。冻结212题逐题输入/选项/标签及原始解析复核通过：总正确157→159，改对3、改错1；训练条件11→12/44，基础训练123→123/135，同模板检查2→3/11，来源组开发21→21/22。安全结果cpt_lora_source_probe_20260915.safe.json。该小范围单轮探针只支持此次配方可测收益很小，不能推出全部材料未学会、语料无用或LoRA整体无效。

正式双评测仍为SC190/226、Log1184/1446，未达196/1224。当前没有新的CPT/SFT训练或来源探针在运行；不重复启动旧614步或已完成探针。下一步恢复S4操作知识候选审查：265段仍非训练就绪，优先来源完整、答案唯一且原模型尚不掌握的条件/计算任务；排除习题答案和官方QA改写，保持来源开发组冻结，先完成来源审核和训练前难度检查，再注册最小训练实验。不能把计划写成已启动。
