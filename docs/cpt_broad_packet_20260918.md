# P4 后的多来源任务扩展与训练价值检查

2026-09-18，用户批准继续。P4 完整测量后仍保留 P1 为参考候选；本轮先落实多来源数据及四顺序检查，不自动把题量增加视为训练收益。[来源、分区与审计](cpt_broad_sources_20260918.safe.json)。

## 冻结范围

| 项目 | 本批 |
|---|---:|
| 训练候选 | 48 |
| 开发候选 | 24 |
| 历史开发来源保持 | 16 |
| 训练能力 | 12 |
| 训练来源单元 / 连通组 | 21 / 6 |
| 其中未用于 P1 的来源单元 | 8 |
| 保持来源单元 / 连通组 | 16 / 5 |

能力包括设施职能、搬运粒度、复核包装发运、活动分布、库位取舍、指标口径、MRP 协同、网络设施与流量、外包管理、库存持有成本、道路载重/容积和铁路轨道/线路。多个来源共享教材、主题和基础规则，不是 21 个统计独立领域，也未检查其是否出现在模型预训练或更早 CPT 中。

训练和开发均覆盖 1—4 个正确选项：训练每种 12 题，开发每种 6 题。初始本地 v1 的开发只覆盖 1/2 个正确项，发现后在任何模型调用前改为 v2；只评测冻结的 v2。保持题的正确项数集中在 2 个，不声称同分布。

训练来源沿用已冻结的历史训练分区；保持来源与其连通组不交叉，且未用于 P1 领域任务和旧回放。开发与训练共享来源规则，留出不同场景或条件组合；其中部分是近邻条件迁移，不应称强结构外推或全新知识测试。

## 数据和答案检查

- 212 条来源引用核对归档原文指纹；另回看本批 8 个未用于 P1 来源的原始证据。
- 对 2,400 个历史、已用诊断及正式问题签名做隔离排重，没有文字/数字规范化后完全相同的问题。正式问题仅在下游排重程序中读取，作者构建程序没有正式题入口；这不证明语义独立。
- 69 项精确等式检查；额外枚举网络连接约束、整件装载、重叠审计的可能成功数及逆推准确率阈值。
- 对每题全部 24 种选项排列重建答案映射，共 2,112 次检查。实际模型只跑四种固定循环排列，保证每个选项出现在每个位置，不把四次回答当作四道独立题。
- 在读取本轮成绩前，人工已隔离 `broad-storage_tradeoffs-idealized_utilization_scope`：题面给出核心公式，适合公式应用诊断，不适合作为领域知识学习的训练候选。推理仍按原88题完整记录，原记录保留，不为凑数补题。
- 29 项相关检查通过。一次测试先受默认临时目录权限影响，改用新的项目内临时目录后全部通过；没有修改测试判据。

## 固定测量

Step120 与 P1 各 88×4=352 次闭卷推理，再由 P1 做 88 次隐藏作者标签的来源复核，共 **792 次**。固定 seed20922、temperature0、thinking off、TP8；闭卷输出最多96 token，盲审最多1536，窗口8192，每批8，max_num_seqs16。来源审查会展示归档来源及场景前提，但不展示作者答案、逐选项解释或目标答案数量。

两模型目录、元数据和分片大小/时间指纹核验后运行；分片未做全量内容哈希。每次调用保存原始输出、结束原因、提示文本/token指纹；结果在本地重建并用第二解析器核对。盲审的反对意见需逐项裁定，模型通过不能自行释放训练数据。

冻结问题文件 SHA-256：`42fa58cd1f04db8448bf8c3a8a6dfb600a5b909425e7c8da2b1a088fdce160e6`。

运行目录：`/workspace/llin-verl-grpo/runs/llin-broad-baseline-20260918-01`。当前状态为基线和盲审运行中；v1.13.123 已知 Step120 为训练180/192、开发85/96、保持61/64，四顺序全对40/48、18/24、13/16。该结果已表明大部分候选内容被基线掌握，需要区分保持锚点与新增学习价值；P1 完成后统一核验和定性。

本轮不执行训练、不重测正式双集、不打开封存题。原始资料与预测留在私有目录，Git 保存可复现作者代码、审计及必要结论。
