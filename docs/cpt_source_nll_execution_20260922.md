# 固定来源答案概率检查

日期：2026-09-22。独立登记：`llin-source-nll-20260922-01`。用户已批准执行；本轮不训练、不重跑正式集、不晋级模型。

最终状态：24/24评分完成、1,428监督token复算通过、未决批次0，交付关闭。结果见[最终复盘](cpt_source_nll_results_20260922.md)。远端状态保留评分完成时的`scoring_complete_analysis_pending`原始快照，本地报告记录后续分析完成状态。

## 固定协议

- Step120、P1、K、R四个既有模型，每模型对原6段完整来源各测一次，共24个评分请求。
- 完全复用K原训练的system/user、assistant全文及结束符，逐token核对原训练输入与监督起点。每模型357个监督token，共1,428个。
- 主指标为逐来源正文平均负对数概率（自然对数，越低越好），标题和结束符单列；同时报告六单元等权平均及token加权平均。
- 当前vLLM接口最少解码1token，因此另登记最多24个解码token，全部丢弃，不作自由回答评价。无新增自由回答、正式评测或训练。
- 六来源、四模型、参数与次数冻结，不按结果补测。未决请求预登记先查清，禁止自动重发。

冻结参数及输入指纹见[登记](cpt_source_nll_registration_20260922.safe.json)。原始内容、输入ID及逐token结果保存在私有目录 `CPT_resources/llin-source-nll-20260922-01`。

## 执行与核验

远端容器：`llin-verl-trainer-m05-20260730`；运行目录 `/workspace/llin-verl-grpo/runs/llin-source-nll-20260922-01`。启动前核对四模型身份、冻结推理代码、输入及运行脚本指纹，通过设备空闲检查并持有共享锁。身份检查沿用配置/分词器/权重索引摘要及权重分片元数据，不等于重新哈希全部权重内容。

协调器逐模型保存请求预登记、原始概率、完成指纹与阶段状态。已完成批次复用；任何故障均保留产物。汇总器重新累计每个token，核对标题/正文/结束符分区及四模型一致性，拒绝错误聚合。合成24请求校验和改坏聚合值的拒绝检查通过。

## 解释边界

这里每一步都提供正确答案前文，测到的是“在正确前文帮助下，后续原文有多可能”。正文概率改善仍可能包含原文措辞熟悉，不能单独证明事实理解、对错误答案的区分、自由调用或迁移。单模型单次确定性测量只作描述，不计算独立样本显著性，不设事后通过阈值。

旧K/R与72次自由调用实验均已封存。本轮结果须与它们联合解释，不能追溯修改原评分。
