# 全量核查方案对照：冻结协议

2026-09-09。用户要求尽快找到有效方法，但不能以局部上涨、挑选样本或成本隐瞒冒充达标。本轮只验证已固定的推理方案，不训练、不制数、不外发评测内容。

## 已有依据与本轮问题

96题四条件诊断已完成1152次本地回答：原指令96预算48/96，原指令2048预算48/96，核查96预算1/96（286/288次截断），核查2048预算58/96（14改对、4改错，18/288次截断）。后者输出171903 tokens，原条件2208，约77.9倍输出量，不等于77.9倍时延。

本轮问题是：原96题上观察到的组合收益，是否能在完整SC知识226题和Logistika1446题中成立？不将96题结果外推为全量收益，也不把短预算截断当知识能力下降。

## 预先锁定的执行口径

- 模型唯一：未经过新SFT的纯教材CPT4x step116；不替换检查点。
- 全量1672题，源SHA256 b652b2108cb552346df11d005c15ff3137c50a756a7b24eb35302683ec33ed99。没有结果筛选、删题、选项重排或资料追加。
- 两条件：原指令+96输出上限，与原四条件诊断完全相同的核查指引+2048输出上限。指引继承原messages_for函数，不再修改措辞。
- 各三轮，总10032回答，最大输出预算10754304 tokens。temperature0、seed1024、BF16、thinking关闭、8192上下文。
- 两个TP8实例各用8卡，按数据集及长候选分层做固定hash顺序交替分片，每片836题。同题两条件、三轮始终同片；每实例32并发，总64。每128题小批交错执行两条件，轮次和分片反转顺序。两条件同配置重新跑，不拿历史分数当同期对照。
- 新分片/并发和过去单实例不完全一致，只使用本轮匹配对照判断收益。两个worker批次耗时之和是计算工作量近似，不等同端到端延迟；另记整体墙钟时间，后续需要服务延迟则单独测量。
- 严格最终答案集合计分，截断与解析无效均计错；三次答案多数票，无有效多数计错。全部重复结果、配对改对/改错、两模型条件均稳定子集、输出量、截断与耗时均保留。

## 决策与停止边界

必须分别报告SC与Logistika，不能用合并分数掩盖一套退步。检查逐题配对不确定性与三轮一致性，不拿三次重复当三个独立题库；题源共享造成的相关性另作限制。不选最好轮次、不改提示刷分、不自动扩大预算。

若没有可信的全量收益，则结束该提示作为主线的尝试；这不等于证明所有提示方法无效。若全量有收益但成本很高，只列为高成本候选，不能直接称达标或直接开始蒸馏。候选的低成本化必须独立授权来源，官方题和本次推理输出不回流训练。

即便领域收益成立，也仍需原Agent与通用保持检查，不能根据本轮领域结果宣称交付通过。用户尚未规定可接受的最终延迟/成本上限，先如实测量，不替用户认定78倍输出可接受。

## 执行与复现

入口`run_supply_full_prompt_comparison_m05.sh`及`run_supply_full_prompt_comparison.py`。唯一运行目录`runs/supply-chain-full-prompt-comparison-20260909-01`；不可变代码`/opt/llin-supply-full-prompt-code-20260909-01`。沿用资源锁，两worker都结束才释放，不覆盖旧结果。worker失败停止完整性晋级，不以另一半结果冒充全量。

`protocol.safe.json`在加载前写入源和脚本hash；每片`progress.safe.json`报告完成数，原始预测仅在服务器；最终`summary.safe.json`自动汇总。三个单元测试覆盖分片完整性/平衡/确定性、无效答案计错、多数票重复身份检查。启动脚本保留Ascend继承的PYTHONPATH，避免再次丢失acl运行库。

后台下一步：检查本轮两片进度，完成后复核全量配对与成本，给出是否值得继续该路线的结论；不自动开始新训练或付费制数。
