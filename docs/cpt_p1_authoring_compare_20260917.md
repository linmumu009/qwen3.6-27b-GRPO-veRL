# P1 对来源题小试的同条件核对（2026-09-17）

## 要回答的问题

上一轮Step120在16道来源题、两套选项顺序上答对25/32，且有选项顺序变化和稳定错误。P1是此前正式双集点估计较好的已有模型（192/1193）。本轮只检查这些具体缺口在P1上是否仍然存在，避免把Step120的错误直接当成P1的补训需求。

## 固定协议

- 复用原16题及作者答案，不改题、不筛掉难题、不加入正式题。
- 复用原直接输出答案索引的提示和两套选项顺序，固定32次调用；不使用上一轮“先真假后索引”提示。
- Step120对照复用已核验的32条原始回答，文件SHA256为 `a6dc2781563b88e519f266ac91c0cfb7f65c6feb1baabb1b32ad4b5e1cb98f46`。
- 题包SHA256为 `fdb9fb716f226fd5c2b6531521ef84e990fca83fae65a29da3f295418e674b26`。
- 温度0、seed20920、关闭思考、输出上限96、上下文8192、TP8、并发上限16，与原对照一致。
- P1使用已完成正式测量的 `llin-transfer-p1-run-20260916-02/training/llin-step120-p1-hf`。加载前核对模型元数据哈希，以及全部分片大小、修改时间；本轮不重复声称对56GB权重做了全量内容哈希。
- 加载前逐条重建P1输入token，并与Step120原记录的token指纹核对；提示或顺序不同则终止。无效、截断回答仍照错计。

远端运行目录：`/workspace/llin-verl-grpo/runs/llin-p1-authoring-run-20260917-01`。

本轮不训练、不修改正式评测、不重复做来源复核。原16题的两道复核索引/解释矛盾及操作者复算记录仍保留在[上一轮报告](cpt_source_authoring_pilot_20260917.md)。

## 核验与限制

10项相关检查通过，覆盖全选解析、逐项答案盲化、选项映射、新增回退、无效与截断输出计错，以及不同输入token或选项顺序被拒绝。结果将保留每次改对/改错、两种顺序都对、顺序变化和稳定同错的区别。

这16题是已经检查过的开发诊断。训练候选与开发共享来源规则；四个能力来源同属一个历史连通组；4道保持题不足以确认广域保持。两个选项顺序不是独立样本。本轮也不回答CPT相对直接SFT的增量收益。

## 已完成结果

32次P1推理全部结束。模型记录、全部输入token对齐、远端token重建、本地评分重建、独立JSON答案解析均通过；零无效、零截断。[完整逐次结果、输入和输出指纹](cpt_p1_authoring_compare_20260917.safe.json)。

| 指标 | Step120 | P1 |
|---|---:|---:|
| 全部调用答对 | 25/32 | 23/32 |
| 训练候选诊断 | 13/16 | 12/16 |
| 开发诊断 | 4/8 | 3/8 |
| 保持诊断 | 8/8 | 8/8 |
| 两个顺序均答对的题 | 11/16 | 10/16 |
| 换顺序后答案集合变化的题 | 4/16 | 5/16 |

P1相对Step120改对1次、新增错3次，净少2次。唯一改对来自“拣选行与多次抓取”的一个选项顺序；另外三个新增错误分别漏掉了正确的密度计算、净成本节省、以及一个符合总量条件的运力反例。三次新增错误均为漏选一个正确项，但这不能证明漏选是训练导致的唯一机制。

P1全部9次错误按答案集合分为：仅漏选3、仅多选3、同时替换3。这些是相关的重复调用，不是9个独立样本。

### 仍然存在的具体缺口

1. **批量成本干预：两个顺序稳定同错。** 三个干预后的成本为78、82、78，相对节省80，只有第一和第三种符合目标；P1两次都只选“无须干预已经符合”。说明不能用简单加减或定义题的正确率代替条件组合检查。
2. **串并行截止时间：两个顺序均错，错法不同。** 串行17分钟超出16分钟截止；P1始终包含错误的“串行满足截止”，其中一次还漏掉正确的“并行满足截止”。
3. **运力逆推：两个顺序均错。** 一次漏掉满足总量约束的反例，另一次包含违反150实际运输总量的反例；需要同时检查局部可行性、总量一致性和反驳目标。
4. **拣选指标与答案完整性：受选项顺序影响。** 单地点多次抓取本身已改善，但固定布局下的活动逆推仍会误把抓取当作独立拣选行，且一个全对题漏选了正确的数值结论。

## 补训内容决定

保留P1作为正式工程指标的已有参考模型；这组小试没有证据要求用Step120替换它。也不把这16题直接加入训练来追求本组得分。

已落盘[来源任务规格](cpt_p1_remediation_spec_20260917.safe.json)：优先编写批量成本、串并行时间/人工作业量、运力约束组合，其次处理拣选密度与完整选项集合。每个能力最多4道新训练候选，另最多2道独立构造的开发候选，总上限16/8；这是工作量上限，**没有生成或放行这些新训练题**，不得为凑数降低来源或语义要求。

编写时逐选项建立真假和理由，再从已经核验的真值生成答案索引；数值推导和选项结论必须相符。源材料仍来自冻结来源包。当前16题、历史诊断和正式题用于下游排重；不进入新训练，不给制题器提供正式问答。

后续放行仍需要逐项来源核验、跨分区语义检查，以及比4道保持题更广的来源隔离检查。仓储、企业流程及真实Agent保持覆盖仍缺；补齐后才冻结实际数据与训练预算。本轮没有启动训练、追加提示搜索或正式测量。
